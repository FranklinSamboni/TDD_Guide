//
//  VowelTests.swift
//  MovieAppTests
//
//  Created by Franklin Samboni on 18/02/21.
//

import Foundation
import XCTest
@testable import MovieApp
/*
class VowelTest2: XCTestCase {
    
    func testVowelCount_IsZero_WhenEmptyInput() {
        //given
        let empty = ""
        
        //when
        let count = try! VowelCount().count(input: empty)
        
        //then
        XCTAssertEqual(count, 0)
    }
    
    func testVowelCount_IsCountCorrectly() {
        // given
        let string = "Hola mundo"
        
        //when
        let count = try! VowelCount().count(input: string)
        
        //then
        XCTAssertEqual(count, 4)
    }
    
    func testVowelCount_ReturnExceptionWhenStringSizeIsMoreThan10Characters() {
        //given
        let string = "una frase demasiado larga"
        
        //when
        XCTAssertThrowsError(try VowelCount().count(input: string), "Deberia mostrar una excepcion")
    }
    
    /*func testVowelCount_With_DifferentsInputs() {
        XCTAssertEqual(try! VowelCount().count(input: ""), 0)
        XCTAssertEqual(try! VowelCount().count(input: "Hola mundo"), 4)
        XCTAssertThrowsError(try VowelCount().count(input: "una frase demasiado larga"), "Deberia mostrar una excepcion")
    }*/
}
*/
